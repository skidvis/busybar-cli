version: 2

# Without this, both the binary and the archives take the repo name
# (busybar-cli).  The command is invoked as "busybar" everywhere.
project_name: busybar

builds:
  - env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ignore:
      - goos: windows
        goarch: arm64
    flags: [-trimpath]
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.updateRepo={{.Env.GITHUB_REPOSITORY}}

archives:
  - formats: [tar.gz]
    format_overrides:
      - goos: windows
        formats: [zip]

checksum:
  name_template: checksums.txt

changelog:
  use: github
